# Veil of Empires Roll20 v2.4.2

Use the files inside `Veil of Empires` in the Roll20 Custom Sheet Sandbox.

This build preserves the v2.4.0 sheet and restores reliable Round Actions tracking. All Skill base fields default to 15 for newly created characters. Existing saved character values are not overwritten.
