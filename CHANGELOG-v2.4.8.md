# v2.4.8

## Fixed
- Additional Protocol slots now use the same `summarizeProtocol()` formatter as the three primary prepared slots.
- Attribute bonuses, Skill bonuses, Temporary Health, and special effects now appear correctly.
- Existing repeating Protocol rows refresh automatically when the sheet opens.
- Repeating-row ID parsing no longer assumes a particular Roll20 row-ID shape.

## Preserved
- Equipment catalog styling and Close control.
- Round Actions, attack counters, banner, portrait, and all prior automation.
